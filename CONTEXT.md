# 地图记忆

地图记忆——一个中国行政区与世界国家地图记忆助手。用户通过多种模式记忆省/地级市与国家，答题数据（正确/错误次数、熟练度）本地存储，排行榜与账号云端共享。

## Language

**地图记忆**:
本站的品牌名与站点自称（中国行政区与世界国家地图记忆助手）。刻意不自称「游戏」——详见 `docs/adr/0004` 的版号风险说明。
_Avoid_: 中国行政区记忆、地图记忆游戏、地理游戏

**大洲**:
世界粒度的第一层范围（AS/EU/AF/NA/SA/OC 六者之一），决定出题池的国家集合与地图取景框。
_Avoid_: 洲、地区

**次区域**:
大洲内部按地理方位划分的第二层范围（东亚、东南亚、南亚、西亚、中亚、东欧、西欧……共 23 个），是国家之上的最小范围单位，也是下钻的**叶子层**。**大洋洲不提供次区域层**：它的四个分区（澳新/美拉尼西亚/密克罗尼西亚/波利尼西亚）仍是数据的一部分，但所有模式都不再下钻到它们——大洋洲本身依然可选。
_Avoid_: 分区、子区域、子地区

**全亚洲**:
次区域行的首项，表示「已选亚洲但不限次区域」——与「全世界」同构的「回到上一层」出口，不是第七个大洲。
_Avoid_: 亚洲全部、整洲

**下钻**:
逐层缩小范围的动作，每层只前进一级：中国侧 全国 → 省；世界侧 全世界 → 大洲 → 次区域。答过题（测试已开始）后不再下钻。两个例外：**唯一层级省级单位**（京津沪渝/港澳台）不下钻、**大洋洲**不再细分次区域。
下钻后**范围之外的其他地区画成什么样**由全局设置**下钻后隐藏无关地区**决定（默认隐藏）；无论哪种取值，它们的**可交互性都不变**（仍是**惰性面**）。
_Avoid_: 钻取、进入、深入

**空白返回**:
点击地图上未命中任何答题单位的区域，退回上一层范围（次区域→大洲→全世界；省→全国）。测验进行中不生效，其余模式生效。
「未命中任何答题单位」**包含当前范围之外的国家面/地级面**：下钻到大洲/次区域后，其余洲的面在几何上依然存在，但它们不参与命中，点击效果与真正的空白完全一致（见**惰性面**）。
_Avoid_: 点击空白、返回上级、退出下钻

**惰性面**:
地图上存在几何、但悬停不高亮、点击不派发任何单位事件的面。当前有两类：**装饰面**（属地/南极/南海诸岛等），以及下钻后位于当前范围之外的面（其他洲的国家面、其他省的地级面）。
之所以需要「保留几何但不响应」而不是直接不画：ECharts 的 geo 组件对未登记外观的面会回落到默认样式，那样它们虽然「看起来没有了」，却仍参与命中测试并触发悬停遮罩——表现为「悬停空白处高亮一个看不见的国家、点击还会跳到它的上级区域」。正确做法是把它们登记为 `silent`，ECharts 对 `silent` region 既不派发鼠标事件也不做 emphasis。
**「可见」与「可交互」是两个正交的轴**：本轮新增的**下钻后隐藏无关地区**只切换范围外面的**可见性**（透明 ↔ 浅灰），`silent` 不变——所以关闭该设置后出现的是「**看得见的惰性面**」：它占着位置、画着浅灰，但照样不高亮、不给 tooltip、点了等同点空白。
_Avoid_: 隐藏面、不可见面、空白面

**极小国家**:
地图上可见面积过小、几乎无法发现与点击的国家。清单由 `scripts/build-subregions.mjs` 依据世界几何面积（度²，与视觉面积一致）现算，判定线为「面积 ≤ 马绍尔群岛」。当前为 6 国：摩纳哥、马尔代夫、马绍尔群岛、瑙鲁、圣马力诺、图瓦卢。
由全局设置**忽略面积极小的国家**控制：开启后这些国家不再出题、地图上灰显且成为**惰性面**。该设置**只影响练习，不影响排行榜范围**（排名池始终按全部答题国，保证开关不同的用户在同一张榜上可比）。
_Avoid_: 小国、微国家、袖珍国

**取名口径**:
题面与地图标签用哪一套名字（2026-09 新增的三组分段按钮）。世界档两组：**国名 / 首都 / 国旗**（考国名、考首都名，还是**给一张国旗图片、点地图上对应的国家**）与**中文 / 英文**（内容语言 —— 只换题面与地图标签的文字，UI 交互文案始终中文，故它不是应用级 i18n）；省级全国一组：**省名 / 省会 / 简称**（省会档题面「石家庄」→ 点/输入河北省）。
只影响**世界全国**与**省级全国**两档范围：地级（市级全国/单省）与无尽闯关沿用单位名。切换与「粒度」同一守卫：只在未开始答题时允许（开始后整组收起），否则当前题的题面与答案会对不上。
判题口径与题面口径**同源**：国名档只认国名、首都档只认首都名、省会档只认省会名（「石家庄」与「石家庄市」都算 —— 那是同一个名字的行政后缀写法）、简称档只认简称（这三者改的是「考什么」，故严格，不容错）；中/英文是同一个名字的两种写法，故两种都接受（语言开关只换显示文字）。
**未开始的浏览标签也跟着口径走**（2026-09 第二轮）：选「首都」就标首都名、选「省会」就标省会名、选「简称」就标单字简称、选「国旗」就**把国旗缩略图画在标签上**（见 `RenderState.browseLabel`）。开始答题后只留**答题标签**；唯一例外是国旗档的已作答标签仍写国名 —— 绿/红反馈要读得懂（刚认出的这面旗是哪国），浏览态才画旗。
**国旗档只在点击模式提供**（输入模式没有"看图点地图"这条路），题面 `alt` 为空以免泄露答案。它需要**预取**：点击模式的下一题本来要答对那一刻才选出，于是点击模式把选题**提前一步**，预取队列恒为「当前题 + 接下来两道」三面（用户 2026-09 口径：只缓存接下来两个国旗）。
_Avoid_: 语言开关、i18n、显示模式、出题模式

**取名口径注册表**:
`src/modes/naming.ts` —— 「考什么名字」这件事的**唯一声明处**。一张表里三组（`world` / `lang` / `province`），每组若干档，每档自己声明：段按钮文字、只在哪些模式提供、题面名（`questionName`）、地图标签名（`labelName`）、题面用图时给什么资源（`questionImage`）、标签用图时给什么缩略图（`labelImage`）、输入判题（`judge`）、输入框占位提示（`placeholderKey`）。
**加一档 = 加一行**：段按钮由 `ui/namingControls.ts` 按表生成（`index.html` 只留空容器），`chromeSync` 按表显隐、`appController` 按表接线、`InputMode` 按表判题、`MapQuizMode` 按表取题面/标签文本。在这张表出现之前，同一件事散在 10 个文件里，加一档「省会」要动 11 个文件，漏一处的表现是界面自相矛盾（题面写省会、标签写省名、判题只认简称）。
⚠ 每档的文本函数**只认自己那一档的取值**，不回头读 `naming.<自己的字段>` —— 否则"这一档的函数"其实取决于调用方传入的整个口径（`naming.test.ts` 有一条不变量钉住这件事）。
_Avoid_: 口径配置、分段按钮配置

**模式目录**:
`src/modes/capabilities.ts` 的 `MODE_SPECS` —— 每个模式一行，声明它的名字（`titleKey`）、有没有帮助说明、是不是计时测验、有没有排行榜资格、有没有「世界/省级/市级」粒度行、「顺序/随机/错题」行的元素 id 与默认顺序、搜索框是常驻还是仅测试中、重置是否弹结算卡片、是不是非地图模式（留言板/管理端）、是不是两阶段（拼图）、是不是熟练度分析。
**加一个模式只改这一行**（`Mode` 类型加 id 后 tsc 会强制你补 spec，`messages.json` 缺 `mode.<id>.title` 也会在 tsc 阶段报错）。`跳过/暂停/重置` 三个按钮的显隐收敛成 `testButtonsVisible()` 一个纯函数（原先散在 `chromeSync` 里三个嵌套三元表达式，读它要同时在脑子里展开四五个模式）。
模式名**只有这一处来源**：模式 tab、每模式设置浮层标题、排行榜标题、开始卡片标题都读它；`index.html` 里 tab 上的中文只是首屏兜底，由 `capabilities.test.ts` 断言与它逐字一致（从前漂过一次：输入模式的设置浮层标题写着「自测模式」，而 tab 写着「输入模式」）。
_Avoid_: 能力表（只说"能力"会漏掉"名字/文案"这一半）、模式注册表

**浏览标签**:
**未开始**（地图上还没有出题、只有开始卡片）时显示的**全量**地名：世界档=国名/首都/**国旗小图**、省级全国=省名/省会/简称、其余（市级全国/单省/省级全国下钻某省）=地级市名，且任何缩放倍率都显示。世界与省级全国那两档的内容**跟着取名口径走**（见上），地级档没有别的叫法故不受影响。
它与**答题标签**是两件事：开始答题后浏览标签收起，已作答单位的绿/红标签保留（那是答题反馈）；结束（结算卡片出现 / 答完 / 重置）后浏览标签复现，**暂停不算结束**。
由导航栏「设置 → 地图标签」的**未开始时显示地图标签**开关控制（默认显示）；拼图模式的名称由难度档说话，不受该开关影响。
_Avoid_: 地名常显、自由浏览（2026-09 前由「自由模式」提供，该模式已下线，浏览能力并入前四个模式）

**答题标签**:
答题**进行中**才出现的标签：已作答单位为绿（答对）/红（答错），文本是当前**取名口径**下的名字；当前题目（蓝色）不显示标签，避免泄露答案。
与**浏览标签**互斥出现的时机不同：前者只在答题中出现，后者只在未开始/已结束时出现。
_Avoid_: 绿色标签、反馈标签

**未开始（浏览态）**:
模式已经进入、但这一局还没开始：地图上还没有出题、顶部是开始卡片。它同时决定三件事：
① 地图显示**浏览标签**；② 题面口径开关（世界档「国名/首都/国旗」「中文/英文」、省级全国「省名/省会/简称」）可用；
③ 左上角按钮区按固定顺序纵向排列 —— 顺序/题面行 → 大洲 → 次区域 → **世界/省级/市级** → **重置（独占最后一行）**。
点「开始」即转入**答题态**：浏览标签与口径开关一起收起，`跳过 · 暂停 · 重置` 回到同一行。
**熟练度分析没有"开始"**，它永远处于浏览态，但其按钮布局不参与 ③（粒度行与重置同行）。
_Avoid_: 待机、空闲、初始状态、准备中

**省级简称**:
省级行政单位的**单字**简称（京、沪、粤、蒙）。官方并存的第二写法（川/蜀、贵/黔、云/滇、陕/秦、甘/陇）**只用于接受输入**，题面与地图标签显示主简称。数据是 `src/province-abbr.json`，与 `units.json` 的 34 个省级单位一一对应（由 `provinceAbbr.test.ts` 断言）。
它与**去后缀省名**是两件事，务必区分：去后缀省名由 `normalizeProvince` 现算（广东省 → 广东、新疆维吾尔自治区 → 新疆），是「省名」档的题面与地图标签口径，也是排行榜范围名称的写法。
_Avoid_: 省名简称、缩写、短名（都会与「去后缀省名」混淆）

**省级省会**:
省级行政单位**政府驻地**那座城市（河北省 → 石家庄、广东省 → 广州）。它是「省名 / 省会 / 简称」中间那一档的题面与地图标签口径：点击模式题面「石家庄」→ 点河北省；输入模式看高亮的省输入其省会名；判题只认省会名（「石家庄」与「石家庄市」都算，那是同一个名字的行政后缀写法），不认省名与简称。
**京津沪渝港澳的「省会」就是它自己**（北京市政府驻北京市）—— 这是事实而不是占位，故不做特判：一旦特判，省会档的题库就会随行政体制变化"少几个省"，反而更难解释。
数据是 `src/province-capital.json`（34 条），与 `units.json` 的 34 个省级单位一一对应（由 `provinceCapital.test.ts` 断言：键集合一致、取值互不重复）。
_Avoid_: 首府（那是自治区的正式叫法，本项目统一用「省会」以免与「首都」混淆）、行政中心

**国旗缩略图**:
地图标签上画的国旗，是 40×30 的 WebP 小图（`public/data/flags/thumbs/*.webp`，194 面合计 163KB），由 `scripts/build-flag-thumbs.mjs` 用无头 Edge 从原始 SVG 栅格化而来。**点击模式的题面卡片仍用原始 SVG**（要清楚、用户口径明确不压缩）。
为什么要两套：标签只有二十几像素宽，且缩放/拖动/答题时每帧都会重画，194 张复杂 SVG 的解析与绘制成本远高于 1KB 位图。缩略图在 zrender 里当**字符串 URL** 画（加载完自动重绘），外面再套一张带描边的卡片 —— 白底国旗（瑞士/日本）直接画在浅色地图上等于看不见。
_Avoid_: 国旗图标、小旗、emoji 国旗（Windows 的 Chrome/Edge 不渲染国旗 emoji）

**答错跟随**:
答错后把镜头平移到**正确答案**所在位置的自反馈动作，**不改变缩放倍率**。所有测验模式通用（含错误回滚的每一次答错）。与**世界跟随**的区别：世界跟随发生在出题时且按面积决定缩放，答错跟随只平移、保留用户当前缩放。
目标已经**舒适可见**（在视口内且距四边不少于**跟随边距**）时，答错跟随**完全不动镜头**——闪红本身已是反馈，没必要为了"看这里"把画面推来推去。
_Avoid_: 自动聚焦、镜头回正、纠正视角

**世界跟随**:
世界粒度出题时的镜头自动跟随。缩放倍率与**国家面积成反比**（面积越小放得越大），按 `A − B·ln(面积)` 绝对映射后夹到 `[3, 28]`：立陶宛约 11x、法国约 8x、俄罗斯约 3x。
用**对数**而不是幂律：用户给的三档锚点在 (ln 面积, zoom) 下不共线（两段斜率差 0.59 倍），幂律无解；在 (ln 面积, zoom) 下则几乎共线。
倍率只依赖国家本身的面积，不随当前范围（全世界/某洲）变化。落点与倍率都受**跟随钳制**与**取景边界**约束。

**取景边界**:
当前视图允许显示的数据矩形（lng/lat 四至）；视口越出它就会露出纯背景色，也就是「半屏空白」。分层取值：
- 全国地级/省级地图 = 中国投影 bbox（73.5–135.1 / 3.4–53.6，即五档几何并集，跨档不移动）
- **下钻某省** = 该省地级单位并集的几何 bbox（此时邻省是透明的，越出这个框就是空白）
- 世界全国 = 世界投影 bbox（-180–180 / -90–83.6）
- 世界**大洲/次区域** = 各自的标定框（`CONTINENT_VIEWS` / `SUBREGION_VIEWS`）——注意这两者是**取景框**而非数据边界，框外仍有陆地。
_Avoid_: 地图范围、视图范围（那是**地图范围/答题范围**的意思）、可视区域

**跟随边距**（或简称**边距**）:
跟随钳制里使用的唯一长度参数：**视口短边的 10%**。一个数承担三件事：允许镜头偏离目标正中的上限、目标落在取景边界上时被容忍的露白上限、以及**答错跟随**判定「已舒适可见」的距离阈值。
_Avoid_: 安全区、留白宽度

**跟随钳制**:
跟随不再无条件把目标放到正中，而是逐轴 `center = clamp( clamp(目标, 边界±半视口), 目标±(半视口−边距) )`：
- 内层保证视口不越出**取景边界**（露白 0）；
- 外层保证目标离屏幕边不少于**跟随边距**；两条冲突时（目标正好落在取景边界上）**外层让步**，允许露白 ≤ 边距。
配合一条**倍率下限**：跟随倍率取 `max(梯子倍率, 取景边界覆盖视口所需倍率)`，夹到 `[0.8, 28]`——否则下钻到小省（宁夏）时视口比省还大，无论怎么摆中心都是空白。越出取景边界的目标（只可能发生在世界大洲/次区域的取景框上）恒留在视口内且离屏幕边恰为边距。
_Avoid_: 居中限制、镜头限制、边界回弹（那是已移除的 `clampPan`）

**顺序出题**:
输入模式的一种出题顺序：**严格广度优先**。维护显式**前沿队列**，取队首出题并把它的所有未作答邻居压入队尾。
「严格」的定义是可断言的：**不可能出现空洞**——空洞指「某个未作答单位的全部邻居都已作答」且它不在队列里。若某单位未作答又不在队列里，说明它没有任何已作答邻居，即它属于另一个连通分量；队列耗尽时从最近的未作答单位重新播种分量（BFS 森林）。
不用「已作答集合是否连通」当判据：走向未作答邻居的贪心游走也满足连通，却照样围出空洞。
_Avoid_: 顺序模式、邻近出题、扩散出题

**测验收起排行榜**:
开始测验时自动收起排行榜侧栏（答题时不占视野），结算或重置后自动展开。
是**临时 UI 状态**，不写回 localStorage——落盘会把「测验中途关掉浏览器」固化成用户偏好。用户手动开关照旧持久化。
「收起」= 给侧栏加 `collapsed`（宽度 0 + 内容 `visibility:hidden`），不是给 `#leaderboard` 加 `hidden`。

**范围哨兵**:
排行榜与进度中表示「非单省」范围的特殊字符串：`__world_nation__`（全世界）、`__continent_XX__`（某大洲）、`__subregion_XXX__`（某次区域）、`__province_nation__`（省级全国）。已写入 D1 与 localStorage，**不可改名**。
_Avoid_: 作用域标识、scope 常量

**落地页**:
构建时从数据生成的静态 HTML 页面（省级/大洲/次区域），供搜索引擎读取地名正文，并通过 URL 参数深链回应用对应视图。
_Avoid_: 静态页、SEO 页、详情页

**熟练度**:
对某个地级单位的掌握程度，由累计答题分数（正确次数 − 错误次数）决定。
_Avoid_: 熟练度得分、掌握分

**熟练**:
熟练度为正（score > 0）的地级单位，即已稳定掌握。
_Avoid_: 掌握、正分

**未知**:
熟练度为零（score = 0）的地级单位，即从未答题或答平。
_Avoid_: 零分、未涉及

**陌生**:
熟练度为负（score < 0）的地级单位，即答错过、需要复习。
_Avoid_: 错题、负分

**熟练度侧栏**:
熟练度分析模式下展示各省熟练度统计的侧边栏。每省一行以「熟练：x 未知：y 陌生：z」统计（x/y/z 为地级单位个数）。
_Avoid_: 成绩面板、统计侧栏

**留言板**:
导航栏中的一个独立视图，用户可发帖与回复。
_Avoid_: 评论区、社区、论坛

**发帖**:
在留言板发布一条新的主题内容（需登录，纯文本，≤200 字）。
_Avoid_: 帖子、发布

**回复**:
针对某条帖子发布的从属内容（需登录，纯文本，≤100 字）。
_Avoid_: 评论、回帖

**待提交成绩**:
模式结束后尚未上传到云端排行榜的一轮成绩；提交时若未登录会先进入登录流程。
_Avoid_: 未提交成绩、挂起成绩

**所在地**:
用户在个人资料中填写的「省 + 地级市」归属地（存为 adcode 对），用于排行榜上用户名与成绩之间的简名展示（如「新疆伊犁」）。
_Avoid_: 家乡、籍贯、所在地

**简名**:
省/地级市的去行政后缀简称，如「新疆维吾尔自治区」→「新疆」、「伊犁哈萨克自治州」→「伊犁」。
_Avoid_: 短名、缩写

**头像**:
用户上传的个人图片，上传时压缩为 ≤20KB 的 JPEG dataUrl，存于用户资料；排行榜与留言板在用户名前显示。
_Avoid_: 图片、图像、相片

**修改密码**:
用户在下拉菜单中更换登录密码的操作（旧密码 + 新密码 + 确认新密码），独立于个人资料编辑。
_Avoid_: 换密码、重置密码

**公告**:
导航栏「公告」按钮弹出的浮层中列出的历史消息（标题 + 正文，纯文本，按时间倒序，全部展开不折叠），所有用户可读。
_Avoid_: 通知、消息、动态

**介绍卡片**:
每次进入站点时弹出的功能简介卡片，内容是置顶公告「站点介绍」；用户可勾选「不再显示」后通过公告栏再次查看。
_Avoid_: 欢迎页、引导页、介绍弹窗

**管理员**:
拥有管理权限的用户（users.is_admin=1），可执行用户查看、日志查看、公告发布/修改/删除。
_Avoid_: 站长、超管、老板

**访问日志**:
记录每次页面访问的明细（时间/IP/UA/是否登录）与按天/小时的访问量统计，仅供管理员查看。
_Avoid_: 日志、访问记录、统计

**输入模式**:
通过键盘输入地名作答的测验模式（「自测」的正式名称）。可选世界/省级/市级粒度，支持跳过、暂停、重置与错误回滚；成绩计入熟练度并可提交排行榜（世界/市级全国按答对题数，省级按用时）。
_Avoid_: 自测模式、打字模式、键盘模式

**点击模式**:
按顶部提示点击对应地图单位作答的测验模式。可选世界/省级/市级粒度，支持跳过、暂停、重置与错误回滚；成绩计入熟练度并可提交排行榜（世界/市级全国按答对题数，省级按用时）。
_Avoid_: 点选模式、鼠标模式

**无尽闯关**:
限时输入地级市名称收集金币的闯关模式（每关 45 秒，按 Enter 确认），金币跨关累计、达标升级、超时结束；答题不改变熟练度。仅支持地级市（中国），不支持世界/省级粒度。
_Avoid_: 闯关赛、金币模式

**熟练度分析**:
按累计答题分数分层着色地图的分析视图，可切换世界/省级/地级粒度（省级双击省下钻查看地级熟练度；世界双击国家逐层下钻到大洲/次区域）；不响应输入、不改变熟练度。三档地图**默认常显地名标签**（不受缩放倍率限制）。世界档的侧栏聚合始终统计全世界 194 国，地图被过滤到某大洲/次区域时另加一行「地图范围」说明。左下角有与其他模式同构的**设置**浮层，其中「隐藏地图标签」可整体关闭三档的地名标签（地级市名/省名/国名）。
_Avoid_: 自由模式、成绩面板、分析视图

**拼图模式**:
把地图拆成碎片、拖拽拼回完整地图的记忆模式。分**两个阶段**：先是**选范围阶段**（画面上仍有完整地图，可切「世界/省级/市级」、下钻大洲/次区域或某省的地级市、点空白退回上一层），出「开始」卡片写明本范围有多少片；点「开始」后地图消失、画布清空，左侧三个**卡槽**各装一片；拖出后碎片是 1:1 真值大小、可放在任意位置；**松手时**相邻两片若已接近其真值相对位置（≤15 拼图像素）便**精确对齐**并为**拼图组**，此后整组一起移动；该范围全部碎片吸成**一整块**即获胜，此时自动补上海岛礁（省级档的三沙）并弹完成卡片。「重置」回到选范围阶段。
_Avoid_: 拼图游戏、拼图闯关（本站自称「助手/记忆/练习」，见 `docs/adr/0004`）

**卡槽**:
拼图阶段左侧那三个玻璃态方槽。每槽装一片尚未使用的碎片（按"最长边占槽 78%"缩放预览），拖出后立刻从抽取池补进新片；槽空即代表所有碎片都已放下。碎片拖出后**不能退回**卡槽。
_Avoid_: 库存、托盘、碎片栏

**拼图组**:
拼图模式下已经拼合、共享同一偏移的一组碎片。组内相对位置就是真值（零缝隙），拖动组内任意一片等于拖动整组；两个组靠近时按同样的相邻规则合并。**组的「面积」= 组内各片面积之和**，画布上下层按它排（见**拼图叠放**）。
_Avoid_: 群组、拼好的块

**拼图叠放**:
拼图碎片的上下覆盖规则：先按**组的总面积**从大到小排（大的在下），同一组内再按**各片自身面积**从大到小排（小的在上）。于是"许多小碎片拼成的大块"会沉到**中层碎片之下**（虽然它每一片都很小），而北京/天津仍压在河北的环里。
_Avoid_: 图层、z-index

**拼图吸附**:
松手时相邻两片对齐成活。**手里那片主动吸附过去**：拖拽方跳到目标组的偏移上对齐，**目标组原地不动**（连锁吸附时依次跳到各目标的位置）。容差**按难度分档**：简单 10px、困难 5px。相邻但超出容差的两片不会吸（继续各占一组）。
_Avoid_: 磁吸、自动拼合

**拼图范围**:
一局拼图的碎片集合，由**粒度 + 下钻层级**决定：世界（全世界 194 个答题国 → 某大洲 → 某次区域）、省级（全国 34 个省级单位）、市级（全国 340 个地级单位 → 某省的地级市）。范围哨兵与点击/输入模式**共用同一套**（`__world_nation__` / `__continent_XX__` / `__subregion_XXX__` / `__province_nation__` / 省 adcode / `null`）。**装饰面**（省直辖县级市、兵团城市、南海诸岛）在任何范围里都不作碎片。
_Avoid_: 关卡、难度、模式

**拼图邻接**:
拼图里"两片可以拼在一起"的关系。来自三处：① 陆地上相邻的单位（国家用 `neighbors`、省级用测验模式的省级邻接聚合、市级用地级单位的 `neighbors`）；② **岛类兜底** —— 零陆地邻居的单位（省级档实测为**海南**与**台湾**，世界档 39 个岛国）各补一个"最近片"，按两片几何 bbox 的最小间距取（省级档台湾→福建、海南→广东）；③ **连通性修补** —— 兜底只给孤立片一条边，图仍可能分成几块（世界档实测 6 块），而**分成几块就永远拼不成一整块**，故按"跨块 bbox 最小间距"逐次搭桥直到全图连通。任何可选范围都保证连通（有解）。
_Avoid_: 相邻省、邻接表

**拼图难度**:
拼图模式的两档设置：**简单**（默认）与**困难**。三处不同：① **名称**——简单显示地名简称、困难一个不显示（选范围阶段作用于地图标签，拼图阶段作用于碎片与卡槽上的名称）；② **吸附容差**——简单 10px、困难 5px；③ **吸附预告**——简单给出只有边界的绿色提示（目标片绿边 + 绿辉光，无整片填充），困难**完全不给**（拖动中/点选幽灵预览都不亮，只有松手后真正吸上才看得见），目的是不允许靠提示"撞运气"。运行中按钮收起、不允许切换，一局结束或回到开始卡片后重新出现。
_Avoid_: 模式、关卡

**拼图排行榜**:
拼图模式的成绩榜。**只有两个范围可提交**：**世界全国**（194 国）与**全国市级**（340 个地级单位）；其余范围（省级全国、大洲、次区域、下钻某省）既不弹结算也不进榜。排名是**已拼个数优先、个数相同比用时**；**已拼至少要 2**（"至少吸上拼成一片"）才算成绩。中途终止同样可以提交：在这两个范围里运行中按**重置**触发结算卡片，或拼完后在完成卡片上点提交。它与答题模式的榜是**两条独立的榜**（同一个范围下，拼图按已拼个数、答题按答对题数），互不影响。
_Avoid_: 拼图得分、拼图金币

**拼图 1x**:
拼图盘的基准缩放倍率。**1x 必须与地图页 zoom=1 是同一比例**（同一窗口下同一单位一样大：中国族约 8.9 px/°、世界族约 2.6 px/°），且拼图的缩放范围与地图一致（0.8–28x）。因此"1x"在拼图里与在其它模式里指的是同一个视觉尺度，左下缩放角标可以直接对照。
_Avoid_: 默认视角、适配倍率

**唯一层级省级单位**:
只有它自己一个下级单位的省级单位：**京津沪渝**（直辖市）与**港澳台**。它们**在所有模式下都不允许下钻**——钻进去只会得到"1 个单位的练习 / 1 片拼图"；点它们只在原地给一行提示，不切换范围。（老存档若停在这类范围上，按全国处理。）
_Avoid_: 小省、单市省

**自由模式**:
自由浏览全部地图单位名称并可缩放、平移的浏览模式，不作答、不计分。沿用与点击/输入模式相同的「世界/省级/市级」分段按钮切换浏览粒度（世界=国名、省级=省名、市级=地级市名），**三档都默认常显地名标签**（不受缩放倍率限制），但**不支持下钻**——双击任何面都不进入下一级；左下角**设置**浮层可整体隐藏标签。
_Avoid_: 浏览模式、观光模式

**熟练度阶梯**:
把累计答题分数映射到七档的分界线集合：**-10 / -5 / -1 / 0 / +1 / +5 / +10**，即糟糕 ≤-10、较差 -9~-5、陌生 -4~-1、一般 0、初识 +1~+4、熟练 +5~+9、炉火纯青 ≥+10。地级/省级/世界三档分析共用这一套断点（`src/modes/analysis.ts` 的 `SCORE_BREAKPOINTS`），地图配色与侧栏档位词都由它推导。
_Avoid_: 色阶阈值、分数区间

**边界深浅**:
全局设置里对三族地图边界分别选取的灰度档位（浅灰色/灰色/深灰色），三族彼此独立：**地级市边界**（地级面描边）、**省级边界**（省界折线）、**世界边界**（世界图中各国家面的描边）。选择保存后立即重绘。
_Avoid_: 边界粗细、线条样式

**下钻后隐藏无关地区**:
全局设置里的一个开关（**默认开**），决定**下钻到次级区域后，当前范围之外的其他地区怎么画**。
- 开（默认，与历史观感一致）：范围外完全不可见（透明惰性面）。
- 关：范围外**保留可见**——填充为**比地图空白底色更深一档的浅灰**（`MapTheme.inactiveFill`，明主题 `#b0b5bd` / 暗主题 `#2b3441`），中国档照画省界、世界档照画国界；但它们**依旧不可交互**（**惰性面**口径不变）。此时下钻某省**不再强制最无损档**，仍按 zoom 走五档精细度。
它只影响**渲染与档位**，不改变练习范围、出题池与排行榜（与**忽略面积极小的国家**同一性质的"观感开关"）。
_Avoid_: 显示其他地区、灰显模式、下钻范围

**五级精细度分段**:
地级与省级各五档的简化阶梯（顶点保留 4%/8%/15%/40%/100%），按 zoom 切换：`<2` ultra、`2~6` pro、`6~10` fine、`10~14` plus、`≥14` lossless（见 `src/map/tiers.ts`）。
默认下钻某省时**强制最精细档**（视口只剩一个省，顶点再多也被裁剪挡住）；**关闭下钻后隐藏无关地区**后，范围外的面仍在画，故此时必须回到按 zoom 分档，否则邻省顶着一整份无损几何参与每帧构建。
_Avoid_: LOD 档位、简化档、精度档

**省级全国**:
输入/点击模式在「省级粒度 + 全国范围」下的视图——出题覆盖全国 34 个省级单元、只计省级熟练度；未开始时点击某省可下钻该省地级市练习，返回后回到省级全国。
_Avoid_: 全国省级、省级总览

**省名标签**:
省级地图上显示的省级简称标签——测验模式（输入/点击）在省级全国仅对已作答省显示（答对绿、答错红，始终显示不随缩放消失）；熟练度分析省级档对全部省名以中性色常显。省级地图下不渲染地级市边界与地级市地名标签。
_Avoid_: 省份标签、省级标签、省名标记

**省级地图**:
省级粒度地图（35 个省面，即 34 个省级单元 + 南海诸岛装饰面），只渲染省级多边形、不渲染地级市行政区，悬浮/点击命中的是整个省。用于输入/点击的省级全国与熟练度分析的省级档。
_Avoid_: 省界图层、省区划

**国家熟练度**:
对国家（194 个答题单位）的掌握程度，记为国家名答题分数（正确次数 − 错误次数），存于独立的第三条熟练度分区（与地级熟练度、省名熟练度完全隔离）。国家名熟练度显示为七档（糟糕/较差/陌生/一般/初识/熟练/炉火纯青），与省/地级七档同阈值。
_Avoid_: 世界熟练度、国家分

**世界地图**:
世界粒度视图（194 个答题国家面 + 若干装饰面如属地/南极），答题国按国家熟练度/答题态着色，装饰面与**极小国家**（开启该设置时）灰显且成为**惰性面**。出题时的镜头行为见**世界跟随**。熟练度分析世界档放大地图超过阈值后才常显国名中性标签。供输入/点击的世界全国与熟练度分析的 world 档使用。中国在世界地图上是**一个合并面**（含台湾/香港/澳门，并含藏南与南海诸岛），与答题单位「国家」一致。
_Avoid_: 全球地图、世界视图

**装饰面**:
世界地图上不属于任何答题国家、只作灰色填充且不响应点击的面（属地、南极、争议地区等）。它**不持有**答题池内的 iso，故不会出现在答题、着色或排行榜中。梵蒂冈移出答题池后也是装饰面——它嵌在意大利的一个内部环（空洞）里，保留灰面正好填住这个洞，若删除几何会在意大利上留下可见缺口。
_Avoid_: 灰面、非答题面、背景面

**藏南**:
中国领土主张范围内的喜马拉雅南麓地区（含达旺、邦迪拉、德让宗、瓦弄等城镇），在世界地图上属于中国合并面。这是**领土口径**的一部分：世界地图的中国面必须包含该地区，图示不得与中国的官方主张相矛盾。
_Avoid_: 阿鲁纳恰尔、南藏、争议区

**世界全国**:
输入/点击模式在「世界粒度 + 全国范围」下的视图——出题覆盖 194 个答题国家单元、只计国家熟练度。作答单位 = 国家（iso_a3 为答题 id）。未开始时单击/双击国家逐层下钻（全世界 → 大洲 → **次区域**）；测试开始后点击国家直接判题，不再下钻。
_Avoid_: 全球全国、世界总览

**国家名标签**:
世界地图上显示的国家名标签——测验模式（输入/点击）在「世界全国」仅对已作答国家显示（答对绿、答错红）；熟练度分析世界档对全部国家名以中性色在放大过阈值后常显。答题目标国名不外泄于地图（当前题不显示标签，点击模式题面在顶部卡片）。
_Avoid_: 国家标签、国名标记、世界标签

**港澳放大框**:
输入/点击模式省级全国左下角的正方形放大区域，将香港、澳门及周边海岸放大显示（广东省面为淡灰底、不参与作答），方便点击这两个小省；主图仍保留港澳两省。说明/缩放按钮位于其正上方。熟练度分析省级档不显示此放大框。
_Avoid_: 港澳小地图、角落插画、港澳面板

**错误回滚**:
输入模式与点击模式的一项设置。开启后，答错时先显示错误提示（红色标记 + 正确答案），然后撤回地图上的红色标记并重新作答同一题（输入模式恢复蓝色当前题高亮、点击模式恢复灰色未作答），反复直到答对才进入下一题。**每一次答错都标红**（不只是第一次），红显时长 1.5 秒。第一次答错计入 wrong 和熟练度错误、进度条保留红格；此后的重复作答无论对错均不计入 correct/wrong 与熟练度、不改变进度条。
标红是**临时高亮**，永久进度红格由第一次答错留在进度条上——两者刻意解耦：前者回答「刚才这次错了」，后者回答「这题错过」。
_Avoid_: 纠错、重答、撤销

## Relationships

- 每个地级单位有一条**熟练度**记录，其符号决定该单位属于**熟练** / **未知** / **陌生** 三态之一。
- 每个省级单元有一条**省名熟练度**记录（省 adcode 为键），独立于地级熟练度分区；每个答题国家有一条**国家熟练度**记录（iso_a3 为键），独立于前两者。
- **熟练度侧栏**按省聚合该省所有地级单位的**熟练** / **未知** / **陌生** 计数；省级档/世界档分别列出各省级单元/各国家的七档档位词。
- **留言板**由多条**发帖**组成；每条**发帖**可有零到多条**回复**。
- 用户在提交成绩时若未登录，则该轮成绩成为**待提交成绩**，登录成功后可继续提交。
- 用户可在个人资料填写**所在地**；排行榜条目据此展示「省**简名** + 市**简名**」，未填写则不展示。
- 每位用户可有零或一张**头像**；排行榜与留言板在用户名前展示该**头像**。
- **修改密码**通过下拉菜单独立完成，不与个人资料编辑耦合。
- 若干条**公告**组成公告浮层；其中一条标题为「站点介绍」的置顶**公告**同时作为**介绍卡片**的内容。
- **输入模式**与**点击模式**都支持世界/省级/市级三种粒度；省级粒度 + 全国范围即**省级全国**，世界粒度 + 全国范围即**世界全国**。
- 世界侧的范围是三级：**世界全国** → **大洲** → **次区域**；**次区域** ⊆ 某一个**大洲**，一个国家恰属一个**次区域**，故**大洲**的国家集合等于其各**次区域**国家集合之并。
- 每个**大洲**都有**次区域**；当一个**大洲**只有一个**次区域**（南美洲）时不显示次区域行——那一行没有可选内容，只会占位。
- 「**全亚洲**」等首项与**世界全国**同构：两者都表示「不限下一层」，而不是某个具体的**次区域**。
- **下钻**在测验开始前可用；**空白返回**在非测验进行时可用。两者互为逆操作，且都会带动分段按钮的高亮回退。
- **惰性面**（范围外的面、**装饰面**、开启设置后的**极小国家**）在命中判定上等价于**空白返回**所要求的「空白」：点它们与点真正的空白效果完全一致（都在有下钻层级时退回上一层）。
- **惰性面**的**可见性**与**可交互性**是两条独立的轴：**下钻后隐藏无关地区**只改前者（范围外面透明 ↔ 浅灰），`silent` 恒定不变。因此「关闭后能看见其他地区」**不等于**它们变成可答题/可下钻的面——灰色区域照旧点不动，点下去等同点空白（退回上一层）。
- **下钻后隐藏无关地区**与**五级精细度分段**有耦合：开启时下钻某省强制最精细档（视口只剩一个省，裁剪已足以挡住顶点开销）；关闭后范围外面参与绘制，故必须退回按 zoom 分档。这条耦合是本轮唯一"改一个开关会改档位"的地方，理由见 `src/map/tiers.ts` 的 `drillForcesLossless`。
- **顺序出题**只在**当前范围**内扩张：只有池子里的单位才可能成为题目、才可能有邻居被压入队列。因此下钻后**不会出到地图上没显示的国家**（越界的题既不出、也点不动），与**惰性面**、**极小国家**两条口径天然一致。
- **世界跟随**与**答错跟随**是两条不同的镜头路径：前者在出题时按面积决定缩放，后者在答错时只平移、保留当前缩放。两条都受**跟随钳制**约束（同一套**取景边界**与**跟随边距**）。
- **拼图模式**的碎片按**拼图邻接**成对拼合：陆地上相邻的两片，或兜底/修补补出来的最近片；每合成一次就产生一个更大的**拼图组**。它的**拼图范围**与点击/输入模式的粒度+下钻共用同一套哨兵，但**不参与答题计分**（无熟练度），只有**拼图排行榜**（世界全国与全国市级两档）记录成绩。
- **排行榜同一范围下按模式分条**：`leaderboard` 表以 `(user_id, mode, scope_province)` 唯一，所以「拼图模式 全国」与「点击模式 全国」是两条独立的榜（前者按**已拼个数**、后者按**答对题数**），互不覆盖。
- **装饰面**（省直辖县级市、兵团城市、南海诸岛）参与地图绘制与**拼图邻接**的计算，但不参与匹配/统计/测试，因此 340 才是答题池的分母；**拼图范围**同样把它们排除。
- **测验收起排行榜**跟着测验生命周期走（三种测验模式共用）；**熟练度分析**侧栏与测验无关，不受影响。
- **极小国家**同时是出题池与交互面的子集关系：开启「忽略面积极小的国家」后，它们既不在 `worldScopedPool()` 里，也是**惰性面**——两条路径必须同时生效，否则会出现「能出题却点不动」或「灰显却仍在池里」的割裂。
- 「忽略面积极小的国家」是**全局设置**，不是每模式设置：三种测验模式与**熟练度分析**共用同一份生效集合（模块级单例），避免各模式各判一套。
- 该设置**不裁剪排行榜范围**：排名池始终按全部答题国，开与不开的用户在同一张榜上可比；成绩单如实标注本次范围。
- **次区域**有自己的答题池、进度键与**范围哨兵**（`__subregion_XXX__`），其排行榜语义与**世界全国**一致（答对题数优先、同数比用时）。
- **省级全国**使用**省级地图**渲染，悬浮/点击命中整个省；左下角**港澳放大框**提供港澳两省的大目标便捷点击；**世界全国**使用**世界地图**渲染。
- **世界全国**只计**国家熟练度**；其成绩提交排行榜语义与市级全国相同（答对题数优先、同数比用时，已答全对即可提交，无须答满 194 国）。**大洲**与**次区域**榜同此语义。
- **世界地图**的中国面必须满足**领土口径**：包含台湾/香港/澳门、**藏南**与南海诸岛；**装饰面**与**极小国家**不得持有答题池内的 iso（梵蒂冈移出后归入装饰面，其 iso 已不在池中）。
- **熟练度分析**省级档使用**省级地图**（无港澳放大框），双击省可下钻查看其地级熟练度；世界档使用**世界地图**，双击国家逐层下钻到大洲与**次区域**，**空白返回**逐层退回。
- **落地页**在构建时从数据生成（34 个省级 + 6 个大洲 + 22 个次区域），页上的地名清单必须与应用一致，并可通过 URL 参数深链回对应视图。
- **无尽闯关**答题不改变**熟练度**；结束成绩计入排行榜（按到达关卡与累计金币）。
- 每次进入站点默认展示**点击模式**。
- **管理员**可发布/修改/删除**公告**、查看**用户管理**与**访问日志**。
- 每次页面访问产生一条**访问日志**记录。

## Example dialogue

> **Dev:** 熟练度侧栏里每省显示的「熟练：3 未知：5 陌生：2」是单位个数还是分数总和？
> **Domain expert:** 是单位个数——该省有 3 个地级单位得分>0（熟练）、5 个得 0（未知）、2 个得负（陌生）。

> **Dev:** 排行榜上一行「张三 新疆伊犁 12个」里的「新疆伊犁」从哪来？
> **Domain expert:** 是张三在个人资料填的所在地，用简名拼接：新疆维吾尔自治区→新疆，伊犁哈萨克自治州→伊犁。

> **Dev:** 上传头像时超过 20KB 会怎样？
> **Domain expert:** 前端自动压缩成 ≤20KB 的 JPEG；实在压不到则提示「头像体积过大」，不保存。排行榜和留言板都会在名字前显示这张头像。

> **Dev:** 介绍卡片和公告栏里的「站点介绍」是两条内容吗？
> **Domain expert:** 不是——介绍卡片就是那条置顶**公告**「站点介绍」。首次弹窗展示它；用户勾选不再显示后，仍可从**公告**浮层随时查看。

> **Dev:** 输入模式省级全国答错一题还能提交成绩吗？
> **Domain expert:** 不能——**省级全国**覆盖的 34 个省级单元必须全部答对才能提交，排行榜只按用时排。

> **Dev:** 世界全国答了 30 国、答对 28 题能提交排行榜吗？
> **Domain expert:** 能——**世界全国**与市级全国同语义：已答全对（wrong=0）即可提交，不必答满 194 国；排行榜按答对题数排，同数比用时。

> **Dev:** 熟练度分析的世界档和省级档在显示国名/省名上有什么区别？
> **Domain expert:** 省级档把所有省名以中性色常显；世界档默认不显示国名——把世界地图放大超过阈值后才以中性色常显，避免 194 个标签糊成一团。两者的测验档（输入/点击）都只对已作答单位显示绿/红标签。

> **Dev:** 我选中了亚洲，次区域行第一项是「全亚洲」——这算第七个大洲吗？
> **Domain expert:** 不算。它是次区域行的「不限下一层」，和第一行的「全世界」同构：选中它表示「范围是亚洲，但不限次区域」。它不会出现在大洲行里。

> **Dev:** 南美洲为什么没有次区域行？
> **Domain expert:** 因为南美洲只划了一个**次区域**（南美）。只有一项的行没有选择余地，只会占位，所以分区数 ≤ 1 时整行隐藏。大洲本身仍然可选。

> **Dev:** 我在东亚做了一半，点空白处会怎样？点回「全亚洲」呢？
> **Domain expert:** 分两种情况。测试**开始前**，点空白或点「全亚洲」都回到亚洲范围（再点一次空白回全世界）。测试**进行中**时点空白会被拒绝并提示「请重置后返回全国」——因为大洲/次区域常常正是答题层，中途换范围等于丢弃本轮。

> **Dev:** 我在东亚答对 5 国、又在全亚洲答对 5 国，排行榜上是同一行吗？
> **Domain expert:** 不是。**次区域**有独立的**范围哨兵** `__subregion_EAS__`，与大洲榜 `__continent_AS__` 是两行。两者排序语义相同（答对题数优先、同数比用时），但成绩互不覆盖。

> **Dev:** 品牌改成了「地图记忆」，为什么 localStorage 里还是 `china-admin-*`？
> **Domain expert:** 那些键是**持久化契约**，不是显示名——改了等于让所有在线用户的本地进度与云端成绩归属失效。改用户看得见的（标题、品牌、文档），不改数据库认得的。详见 `docs/adr/0004`。

> **Dev:** 我下钻到东亚，鼠标移到地图上欧洲那块「空白」处，为什么以前会高亮一个看不见的国家？
> **Domain expert:** 因为那些面只是**没有外观**，几何仍然存在。大洲/次区域视图下其余洲的国面被跳过了外观登记，于是回落到 geo 的默认样式——看不见，但照样参与命中测试、照样触发悬停遮罩，点下去还会跳到它所属的大洲。现在它们被登记为**惰性面**（`silent` + 透明），悬停不高亮、点击等同于点空白。

> **Dev:** 那点这些「空白」处现在会怎样？
> **Domain expert:** 与点真正的空白完全一样：有下钻层级时退回上一层（东亚 → 亚洲 → 全世界）。这正是**空白返回**要的效果——空白就是空白，不该因为你点在哪一块而行为不同。

> **Dev:** 我把「下钻后隐藏无关地区」关掉之后，那些浅灰的省份能点吗？
> **Domain expert:** 不能。这个开关只改**可见性**（透明 ↔ 浅灰），不改**可交互性**——它们仍是**惰性面**：悬停不高亮、没有 tooltip、点了等同点空白（有下钻层级时退回上一层）。它的用途是让你看清「现在练的是哪一块」，而不是把练习范围扩大：出题池、熟练度与排行榜一如既往只算当前范围。

> **Dev:** 关掉它之后，为什么精细度档位也跟着变了？
> **Domain expert:** 因为范围外的面这时也在画。开着的时候视口里只剩当前省，顶点再多也被视口裁剪挡住，所以下钻直接给最无损档；关掉后邻省也参与绘制，若还强制无损档会明显掉帧，于是退回按 zoom 走五档——就是用户口径里的「关闭后下钻到省份，依然保持五级精细度分段」。

> **Dev:** 梵蒂冈为什么是灰的，而不是直接删掉？
> **Domain expert:** 因为它嵌在意大利的一个内部环（空洞）里。删掉它的几何会在意大利上留下一个可见缺口；保留为**装饰面**既填住了洞，又满足「不出题、不可交互」。它的 iso 已不在答题池，所以不违反「装饰面不得持有答题池 iso」这条不变量。

> **Dev:** 我开了「忽略面积极小的国家」，排行榜会不会变成另一个榜？
> **Domain expert:** 不会。该设置**只影响练习**：出题池变小、这些国家灰显不可点，但排行榜范围仍是全部答题国，开与不开的用户在同一张榜上可比。成绩单会如实标注本次范围。

> **Dev:** 「面积极小」的线是怎么定的？为什么是这 6 个？
> **Domain expert:** 按**视觉面积**（经纬度平面度²，与屏幕上看到的大小一致，而不是球面真实面积——后者会把高纬度国家折算得过小）。判定线落在天然断崖上：卡在「≤ 马绍尔群岛」，下一个国家科摩罗的面积是它的 1.7 倍。这个阈值不硬编码清单，构建期从几何现算并断言。

> **Dev:** 错误回滚开着的时候，我第五次答错同一题，还会标红吗？
> **Domain expert:** 会。**每一次答错都标红**，红显 1.5 秒。但只有第一次答错计入 wrong/熟练度/进度红格——标红是「刚才这次错了」的临时反馈，进度红格是「这题错过」的永久记录，两者刻意解耦。

> **Dev:** 世界输入模式答错后，镜头会拉近吗？
> **Domain expert:** 不会。答错时执行的是**答错跟随**——只平移到正确答案位置、保持当前缩放。会改变缩放的是**世界跟随**，那是出题时的行为，且缩放与国家面积成反比（面积越小放得越大）；而且如果正确答案本来就已经**舒适可见**，答错跟随连平移都不做。

> **Dev:** 我在输入模式下答到黑龙江最北那个单位，镜头却把它放在屏幕靠上而不是正中，是不是坏了？
> **Domain expert:** 没坏，那是**跟随钳制**。它原来会把目标硬顶到正中，于是屏幕上半截全是**取景边界**之外的空白（那个单位就压在边界上）。现在镜头只推到「视口不越出边界 + 目标离屏幕边不少于**跟随边距**」为止：目标偏离正中、贴边一侧的地图仍然铺满屏幕，最多只让边距那么宽的空白出现。同理，下钻到宁夏这种小省时视口本来比省还大，无论怎么摆都是空白——所以跟随倍率还有一条下限，会把它抬到能盖住视口（宁夏 12x → 顶到 28x 上限，仍会剩一条空白，因为 28x 是硬上限）。

> **Dev:** 我下钻到亚洲做输入练习，出的题却是一个欧洲国家，地图上根本没显示它——为什么？
> **Domain expert:** 旧实现找「下一个题的邻居」时用的是**全部 194 国**的表，而不是当前范围过滤后的池子，于是顺着国家邻接关系跑到了别的洲。现在**顺序出题**只在本池内扩张，越界的题既不会出、也点不动。

> **Dev:** 顺序模式的出题顺序看起来会围出空洞——一圈答完了，中间还剩一个没答。
> **Domain expert:** 那是旧实现的**贪心游走**造成的：它在邻居里随机挑、挑不到就跳到最近的一个，于是走远了就把身后的单位围住。BFS 不可能这样——**顺序出题**维护一条前沿队列，取队首出题并把它的所有未作答邻居压入队尾，所以未作答又不在队列里的单位一定是另一个连通分量。注意判据不是「已作答集合是否连通」：贪心游走也满足连通，却照样围出空洞。

> **Dev:** 开始答题后排行榜为什么自己收起来了？我手动展开会被记住吗？
> **Domain expert:** **测验收起排行榜**是刻意的——答题时不占视野，结算或重置后自动展开。它不写回 localStorage：那是临时 UI 状态，不是你的偏好。你手动开关才会被持久化。

> **Dev:** 拼图模式里我把台湾拖到福建旁边，为什么会"吸"上？它明明没有陆地邻居。
> **Domain expert:** 那是**拼图邻接**的岛类兜底：零陆地邻居的省级单位（海南、台湾）各按**几何 bbox 最小间距**补一个"最近片"，台湾的最近片是福建、海南的是广东。注意度量用的是 bbox 间距而不是中心点直线距离——海南那种"主岛 + 散落岛礁"的 feature，中心点会落在奇怪的地方。
>
> **Dev:** 为什么海南的碎片只有主岛，南海那些岛礁去哪儿了？
> **Domain expert:** 海南省在省级几何里是一个含 204 个多边形的 feature（三沙一路到 3.84°N，bbox 9.2°×16.3°），整片当碎片会是一块"稀疏大块"。所以**卡槽与拼图只放主岛及近岸小岛**，远海岛礁留到**获胜后自动补上**（淡入并归入海南所在组），玩家不需要拼它们。

> **Dev:** 世界档有澳洲、新西兰、日本这些岛，它们跟谁"相邻"？会不会有的碎片永远吸不上？
> **Domain expert:** 会，如果不额外处理的话。岛类兜底先给每个零陆地邻居的片面补一个最近片（日本→韩国、新西兰→澳洲…），但兜底只给**一条**边，整张图仍可能分成几块 —— 实测世界档 6 块。**分成几块就永远拼不成一整块**，所以**拼图邻接**还有第三层：按"跨块 bbox 最小间距"逐次搭桥直到全图连通。任何可选范围都保证有解。

> **Dev:** 立陶宛 11x、法国 8x、俄罗斯 3x 这三个数字能同时满足吗？
> **Domain expert:** 能，但只能用**对数**。用幂律无解——把三档锚点放到 (ln 面积, ln zoom) 里看，相邻两点连线的斜率分别是 −0.1551 与 −0.2643，差了 0.59 倍，不在一条直线上；换成 (ln 面积, zoom) 坐标则几乎共线（−1.4615 与 −1.3475）。

## Flagged ambiguities

- 「熟练度输入模式」曾被用来同时指「熟练度分析模式」与「输入模式」——已解决：任务改的是**熟练度分析**模式的**熟练度侧栏**，与「输入模式」（自测）无关。
- 「正确：x 错误：y」曾作为侧栏格式候选——已解决：最终采用**熟练 / 未知 / 陌生**三态计数，用户明确否决了正确/错误两态。
- 「世界熟练度」曾被用来同时指「国家名熟练度」分区与「世界地图视图」——已解决：分区术语固定为**国家熟练度**（194 国，iso 键），视图术语固定为**世界地图**/粒度「世界」。
- 「装饰面个数」曾被写进**世界地图**的定义（先是 44 个，换源后变成 49 个）——已解决：装饰面的**数量不是契约**（它随几何源变化，且数量守恒曾经掩盖了藏南丢失这类领土口径回归），故从定义中移除，只保留「不持有答题池内的 iso」这一条不变量。
- 「藏南」最初只被当作一个**几何精细度**问题（边线太粗）——已解决：它是**领土口径**问题。标准 Natural Earth 源把该地区划给印度，故必须用中国视角变体；详见 `docs/adr/0003`。
- 「游戏」曾被用作站点自称以强化传播——已解决：站内一律用「记忆助手 / 记忆 / 练习」。2009 年通知的适用范围明文含**网页游戏、休闲游戏**且未设商业门槛，故免费也不必然免于版号要求；详见 `docs/adr/0004`。
- 「北亚」曾作为俄罗斯所在的**次区域**候选——已解决：**没有北亚**。俄罗斯整体归入**东欧**，因为北亚不是独立的记忆单元，而俄罗斯跨欧亚两洲，按方位粗分比按乌拉尔山切分更贴合「记住国家位置」的目标。
- 「次区域」一度被写成大洲的显示变体（共用大洲榜）——已解决：次区域有独立的答题池、进度键与**范围哨兵**。共用会让「东亚答对 5 国」与「全亚洲答对 5 国」挤进同一行。
- **次区域**划分曾考虑采用联合国 M49 官方分区——已解决：改用方位式粗分（东亚/东南亚/南亚/西亚/中亚）。M49 的二级分区过多且若干归属与国内地理教材口径有出入，不适合「一次记完一个小范围」的目标。
- **大洲**归属曾与**次区域**归属矛盾：土耳其与塞浦路斯原记为大洲 EU，却连同高加索三国归入**次区域**「西亚」。已解决：把两者的大洲改成 AS，使「次区域 ⊆ 大洲」成立；`scripts/fetch-world-data.mjs` 的 `CONTINENT_OF` 已同步，重新生成数据时构建断言会守住这条不变量。
- 「README 说 340 个、而 `units.json` 有 373 条」曾被当作数据不一致——已解决：**两者都对且口径不同**。373 条含 33 个**装饰面**（省直辖县级单位与南海诸岛，只作灰色填充、不参与答题），真实记忆单位为 340 个（333 个地级行政区 + 7 个整体单位）。README 原文已写明这一区分。
- 「装饰面不参与答题」这条口径曾**只在文档里成立**：`data.ts` 的 `isPureDecoration()` 只认南海诸岛，把 2026-09 数据管线给 32 个县级/兵团面标的 `decorative: true` 洗成了 `false`，运行时答题池是 **372** 而不是 340（出题会问"仙桃市"）。已解决：装饰面判定改为**数据标注优先**（`adcode === '100000_JD' || decorative === true`），并加 `src/data.test.ts` 守住 33/340 这条口径。
- 「拼图 1x」曾被理解成"当前范围铺满视口"（覆盖全屏才好看）——已解决：用户口径是**1x 必须与地图 1x 同一比例**（同一窗口下同一单位一样大），下钻小范围时靠滚轮放大（上限与地图一致 28x）。
- 「拼图不进排行榜」曾是明确口径——已解决（2026-09 用户改口）：拼图**在全世界与全国市级两档进榜**，按**已拼个数**排名、同数比用时，其余范围不进榜。它与答题榜是同一张表里 `mode` 不同的两条独立记录。
- 「空白区域不提供任何交互」与上一轮定下的**空白返回**曾看似冲突——已解决：**保留空白返回**。用户的缺陷描述里「点击后下钻到所属上级区域」指的是「点到看不见的国家面、于是跳到该国所属的大洲/次区域」，属于**惰性面**该修的那一类；用户确认空白点击仍应逐层返回。
- 「答错的地点」在被写下时有两种读法：**正确答案**的位置，还是**用户点错**的位置——已解决：取**正确答案**的位置。这是记忆练习的纠错目的（「你刚才该点的是这里」），点错的位置用户本来就看得见。
- 「极小国家」的判定口径曾考虑用**球面真实面积**——已解决：改用**经纬度平面度²**（与屏幕上看到的大小一致）。球面面积会把高纬度国家折算得过小，与「很难发现」这个实际体验不符。
- 「忽略面积极小的国家」曾是**每模式设置**的候选（各模式各存一个开关）——已解决：做成**全局设置**。这是跟随用户而非跟随模式的偏好；做成每模式会出现「输入模式关了、点击模式还开着」的不一致，且四条读取路径（渲染器 + 三模式的池）需要共用同一份生效集合。
- 「梵蒂冈」的移除方式曾考虑**直接删几何**——已解决：改为**保留灰面**。它嵌在意大利的内部环（空洞）里，删几何会在意大利上留下缺口；保留为装饰面同时满足「不出题」与「不可交互」，且不破坏地图完整性。
- 「空洞」一度被理解成「已作答集合不连通」——已解决：**判据是「某个未作答单位的全部邻居都已作答」**。贪心游走每步都走向未作答邻居，已作答集合天然连通（一条路径），却照样能把单位围住；用连通性当判据会漏掉真实缺陷。
- 世界跟随的曲线形状在本轮改了口径：上一轮锚点（法国 13x / 俄罗斯 2x）推出幂律 `K/√面积`，本轮锚点（立陶宛 11x / 法国 8x / 俄罗斯 3x）在幂律下**无解**（两段斜率差 0.59 倍），只能用对数 `A − B·ln(面积)`。已解决：以本轮锚点为准。
- 「极小国放 28x」与「法国 8x / 俄罗斯 3x」不可兼得——已解决：以本轮锚点为准，极小国落在 18–20x，28x 上限实际用不到。
- 「测验收起排行榜」曾被考虑**写进 localStorage**——已解决：**不落盘**。落盘会把「测验中途关掉浏览器」固化成用户偏好，下次进来侧栏是关的，而用户从没这么选过。
